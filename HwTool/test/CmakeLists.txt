set(TEST_SOURCES
    test_main.cpp
)

add_executable(hwtool_tests ${TEST_SOURCES})

target_link_libraries(hwtool_tests
    PRIVATE
        hwtoollib
        GTest::gtest_main
)

target_include_directories(hwtool_tests
    PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${CMAKE_CURRENT_SOURCE_DIR}/../src
        ${CMAKE_CURRENT_SOURCE_DIR}/../include
)

include(GoogleTest)
gtest_discover_tests(hwtool_tests)